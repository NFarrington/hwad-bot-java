allprojects {
    buildscript {
        repositories {
            jcenter()
        }
    }

    repositories {
        jcenter()
    }

    configurations.all {
        resolutionStrategy {
            failOnVersionConflict()
        }
    }
}
